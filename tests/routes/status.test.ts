import test from '../ava';

// test('GET /status should return ok', async t => {
//   const result = await t.context.api().get('/status');

//   t.deepEqual(result.data, {
//     status: 'Ok'
//   });
// });


test('check', t => t.pass());